import React from "react";
import { Card } from 'antd';

const Index = ({ }) => {
    return (
        <Card title='动态IP' style={{ height: '100%'}}>
            动态IP地址
        </Card>
    )
};

export default Index